# Load a list of sccsAnalysis from file

Load a list of objects of type `SccsAnalysis` from file. The file is in
JSON format.

## Usage

``` r
loadSccsAnalysisList(file)
```

## Arguments

- file:

  The name of the file

## Value

A list of objects of type `SccsAnalysis`.
