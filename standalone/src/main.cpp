#include <iostream>
#include <cstdlib>
#include <Rcpp.h>

#include "SccsConverter.h"

using namespace Rcpp;
//#ifdef ECLIPSE

int main() {
	using namespace ohdsi;
	using namespace ohdsi::sccs;
//	IntegerVector a(10);
//	IntegerVector b(10);
//	IntegerVector c(10);
//	DataFrame x = DataFrame::create(Named("a") = a, Named("b") = b, Named("c") = c);
//	IntegerVector d(10);
//	IntegerVector e(10);
//	DataFrame y = DataFrame::create(Named("d") = d, Named("e") = e);
//	List result = SccsConverter::convertToSccs(x,y,0,0,false);
	//std::cout << result << "\n";
	return 0;
}

//#endif
